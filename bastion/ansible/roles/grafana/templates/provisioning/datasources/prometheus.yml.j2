apiVersion: 1

datasources:
  - name: Prometheus
    uid: prometheus
    type: prometheus
    access: proxy
    url: "https://prometheus-proxy.{{ network.domain }}"
    isDefault: true
    editable: false

---
- name: Validate Jellyfin lifecycle state
  ansible.builtin.assert:
    that:
      - jellyfin_state in ['present', 'absent']
    fail_msg: "jellyfin_state must be present or absent."

- name: Reconcile Jellyfin lifecycle
  ansible.builtin.include_tasks: "{{ jellyfin_state }}.yaml"

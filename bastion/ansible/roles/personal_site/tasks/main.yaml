---
- name: Validate personal-site lifecycle state
  ansible.builtin.assert:
    that:
      - personal_site_state in ['present', 'absent']
    fail_msg: "personal_site_state must be present or absent."

- name: Reconcile personal-site lifecycle
  ansible.builtin.include_tasks: "{{ personal_site_state }}.yaml"
